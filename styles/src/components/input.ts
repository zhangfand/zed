import { background } from "../styleTree/components"
import { ColorScheme, Layer } from "../themes/common/colorScheme"
import { withOpacity } from "../utils/color"

export const input = (colorScheme: ColorScheme, layer: Layer) => {
    return {
        //
    }
}
